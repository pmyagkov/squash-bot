version: '3.8'

services:
  bot:
    build: .
    container_name: squash-bot
    restart: unless-stopped
    env_file:
      - .env.prod
    ports:
      - '3010:3010'
    volumes:
      - ./src:/app/src
    command: npm run dev
    environment:
      - NODE_ENV=development

