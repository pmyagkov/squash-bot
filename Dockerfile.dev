# Development image with pre-installed production dependencies
# Used by docker-compose.dev.yml for bot and db-init services
# Source code is mounted as a volume at runtime

FROM node:22-alpine

WORKDIR /app

COPY package*.json ./

RUN npm ci --omit=dev --ignore-scripts