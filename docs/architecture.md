# Squash Payment Bot — Архитектура и требования

## Обзор

Telegram-бот для управления платежами за сквош-занятия в комьюнити. Автоматизирует запись на занятия, расчёт стоимости и трекинг оплат.

---

## Бизнес-контекст

- Комьюнити с абонементом на 16 кортов
- Стоимость корта: **2000** (настраиваемо)
- Регулярные занятия: вторник и суббота
- Обычно 2-10 человек на занятии
- Часовой пояс: **Europe/Belgrade**

---

## Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                         n8n                                  │
│  [Schedule: */15 мин] → [HTTP: POST /bot/check-events]      │
│  [Schedule: раз в день] → [HTTP: POST /bot/check-payments]  │
│  [Schedule: */5 мин] → [HTTP: GET /bot/health] → [Alert]    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Бот (TypeScript)                          │
│  - Telegram API (вся коммуникация)                          │
│  - Notion API (данные)                                       │
│  - Бизнес-логика                                            │
│  - REST endpoints для n8n                                    │
└─────────────────────────────────────────────────────────────┘
```

**Разделение ответственности:**
- **n8n:** Только планировщик — дёргает бота по расписанию, мониторит health
- **Бот:** Вся логика + единственный, кто пишет в Telegram
- Если бот мёртв → n8n шлёт алерт админу

---

## Основные сущности

### Scaffold (шаблон занятия)
Регулярное расписание, порождающее конкретные события.

### Event (занятие)
Конкретное занятие с датой, участниками, оплатами. Создаётся из scaffold автоматически или вручную.

### Participant (участник)
Член комьюнити. Идентифицируется по Telegram username (если есть) или «Имя Фамилия».

### Payment (платёж)
Запись об оплате участника за конкретное занятие.

---

## Telegram-чаты

| Чат | Назначение |
|-----|------------|
| **Основной** | Анонсы, запись, оплаты — для всего комьюнити |
| **Тестовый** | Тестирование бота командами (только админ) |
| **Технический** | Логи всех действий бота и пользователей |

---

## Сценарии использования

### Жизненный цикл занятия
1. Создание scaffold (шаблона)
2. Автоматическое порождение event из scaffold
3. Создание event вручную (ad-hoc)
4. Анонс event в чат
5. Запись участников (inline-кнопки)
6. Завершение занятия — фиксация кортов
7. Сообщение об оплате (сразу после завершения)
8. Отметка оплаты участниками
9. Напоминание должникам (на следующий день)

### Административные
10. Изменение настроек (через Notion)
11. Просмотр истории / долгов
12. Тестовые команды

---

## Сценарий 1: Создание scaffold

**Актор:** Админ

**Команды:**

```
/scaffold add <day> <time> <courts>
/scaffold list
/scaffold toggle <id>
/scaffold remove <id>
```

**Примеры:**
```
/scaffold add Tue 21:00 2
→ Создан шаблон sc_1: Tue 21:00, 2 корта

/scaffold list
→ sc_1: Tue 21:00, 2 корта, активен
→ sc_2: Sat 18:00, 3 корта, неактивен

/scaffold toggle sc_2
→ sc_2 теперь активен

/scaffold remove sc_1
→ sc_1 удалён
```

**Структура данных Scaffold:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | string | Автогенерируемый ID (sc_1, sc_2, ...) |
| day_of_week | enum | Mon, Tue, Wed, Thu, Fri, Sat, Sun |
| time | time | Время начала (HH:MM) |
| default_courts | number | Кортов по умолчанию |
| is_active | boolean | Активен ли шаблон |
| announce_hours_before | number | За сколько часов создавать event (по умолчанию: см. логику ниже) |

**Логика создания event из scaffold:**
- По умолчанию: в 12:00 предыдущего дня
- Минимум: за 24 часа до занятия
- Граничный случай (меньше 24 часов до занятия): создать event сразу

**Пример:**
- Scaffold: вторник 21:00
- Событие создаётся: понедельник 12:00
- Если сейчас понедельник 20:00 и scaffold только создан → event создаётся сразу

---

## Сценарий 2: Порождение event из scaffold

**Триггер:** n8n вызывает `POST /check-events` каждые 15 минут

**Логика:**
1. Бот получает все активные scaffolds
2. Для каждого scaffold проверяет: есть ли event на ближайшую дату?
3. Если нет и пора создавать (по правилу announce_hours_before) → создаёт event
4. Сразу после создания — анонсирует в чат

**Проверка дубликатов:** по связке `scaffold_id + datetime`

**Статусы event:**

| Статус | Описание |
|--------|----------|
| created | Создан, не анонсирован |
| announced | Анонсирован в чат |
| cancelled | Отменён |
| finished | Занятие прошло (1.5 часа после начала), ожидает finalize |
| finalized | Рассчитан, отправлено сообщение об оплате |
| paid | Все участники оплатили |

**Действия (методы):**
- `createEvent(scaffold)` → создаёт event со статусом `created`
- `announceEvent(event)` → постит в Telegram, меняет статус на `announced`

Для scaffold-based events оба действия выполняются подряд в `/check-events`.

---

## Сценарий 3: Создание event вручную (ad-hoc)

**Актор:** Любой участник

**Цель:** Создать занятие вне расписания

**Команды:**

```
/event add <date> <time> <courts>
/event list
/event announce <id>
/event cancel <id>
```

**Форматы даты:**
- Абсолютный: `2024-01-20`
- Относительный: `tomorrow`, `sat`, `next tue`

**Примеры:**
```
/event add 2024-01-20 19:00 2
→ Создан event ev_15 (Sat 20 Jan 19:00, 2 корта). Для анонса: /event announce ev_15

/event add tomorrow 19:00 2
→ Создан event ev_16 (Sun 21 Jan 19:00, 2 корта). Для анонса: /event announce ev_16

/event list
→ ev_15: Sat 20 Jan 19:00, 2 корта, created
→ ev_16: Sun 21 Jan 19:00, 2 корта, announced

/event announce ev_15
→ Анонс отправлен в чат

/event cancel ev_15
→ Event ev_15 отменён. Уведомление отправлено в чат.
```

**Логика отмены:**
- Любой участник может отменить любой event
- Статус меняется на `cancelled`
- Если event был анонсирован — в чат отправляется сообщение об отмене

---

## Сценарий 4: Анонс event

**Триггер:** 
- Автоматически после создания из scaffold (в `/check-events`)
- Вручную командой `/event announce <id>`

**Формат сообщения:**
```
🎾 Сквош: Вторник, 21 января, 21:00
Кортов: 2

Участники:
(пока никого)

[Я иду] [Не иду]
```

**Действия при анонсе:**
1. Распинить предыдущие анонсы events (если есть)
2. Отправить сообщение с inline-кнопками
3. Запинить новое сообщение
4. Сохранить `telegram_message_id` в event
5. Изменить статус на `announced`

---

## Сценарий 5: Запись участников

**Триггер:** Клик по inline-кнопкам под анонсом

**Логика кнопок:**
- «Я иду» — каждый клик +1 к участиям пользователя
- «Не иду» — каждый клик −1 (минимум 0, при 0 пользователь исчезает из списка)

**Обновление сообщения после клика:**
```
🎾 Сквош: Вторник, 21 января, 21:00
Кортов: 2

Участники:
@pasha (×2), @vasya, @petya

[Я иду] [Не иду]
```

**Идентификация участника:**
- По Telegram username (если есть): `@pasha`
- Если нет username: «Имя Фамилия»

**Действия при клике:**
1. Определить пользователя (создать в Participants, если новый)
2. Найти или создать запись в EventParticipants
3. Обновить поле `participations` (+1 или −1)
4. Если participations = 0 → удалить запись из EventParticipants
5. Обновить текст сообщения в Telegram
6. Логировать действие в технический чат

---

## Сценарий 6: Завершение занятия

**Кнопки под анонсом (полный набор):**
```
[Я иду] [Не иду] [+🎾] [-🎾] [✅ Finalize]
```

**Логика кнопок кортов:**
- `+🎾` — увеличить количество кортов на 1
- `-🎾` — уменьшить количество кортов на 1 (минимум 1)

**Жизненный цикл event после анонса:**

1. **До начала занятия:** участники записываются, корты можно менять
2. **Через 1.5 часа после начала:** статус автоматически меняется на `finished`, начинают слаться напоминания
3. **Напоминания:** каждые 2 часа в общий чат «Event не рассчитан»
4. **Finalize:** любой участник нажимает ✅, отправляется сообщение об оплате, напоминания прекращаются

**Кнопки после finished:**
- «Я иду» / «Не иду» продолжают работать (корректировка участия)
- `+🎾` / `-🎾` продолжают работать (корректировка кортов)
- Изменения отражаются в сообщении об оплате

**Статус после Finalize:** `finalized`

**Обновлённые статусы event:**

| Статус | Описание |
|--------|----------|
| created | Создан, не анонсирован |
| announced | Анонсирован в чат |
| cancelled | Отменён |
| finished | Занятие прошло (1.5 часа после начала), ожидает finalize |
| finalized | Рассчитан, отправлено сообщение об оплате |
| paid | Все участники оплатили |

---

## Сценарий 7: Сообщение об оплате

**Триггер:** Нажатие кнопки ✅ Finalize

**Формат сообщения:**
```
💰 Оплата за сквош: Вторник, 21 января

Кортов: 3
Стоимость корта: 2000

@pasha (×2) — 2000 ₽
@vasya — 1000 ₽
@petya — 1000 ₽

[Оплатил ✓]
```

**После оплаты (нажатия кнопки):**
```
💰 Оплата за сквош: Вторник, 21 января

Кортов: 3
Стоимость корта: 2000

@pasha (×2) — 2000 ₽ ✓
@vasya — 1000 ₽
@petya — 1000 ₽ ✓

[Оплатил ✓]
```

**Логика:**
- Кнопка «Оплатил» одна на всех
- При нажатии — определяем пользователя, ставим ✓ рядом с его именем
- Обновляем сообщение
- Создаём запись в таблице Payments (is_paid = true, paid_at = now)
- Когда все оплатили — статус event меняется на `paid`

**Корректировка участия после finalize:**
- Кнопки в анонсе продолжают работать
- Изменения отражаются в сообщении об оплате (пересчёт сумм)
- Если участник хочет «отменить» свою оплату — он выписывается из ивента

---

## Сценарий 8: Отметка оплаты

**Триггер:** Нажатие кнопки «Оплатил ✓» под сообщением об оплате

**Кнопки под сообщением об оплате:**
```
[Оплатил ✓] [Отменить оплату ✗]
```

**Логика «Оплатил»:**
1. Определить пользователя
2. Найти его запись в Payments для этого event
3. Установить is_paid = true, paid_at = now
4. Обновить сообщение (добавить ✓ рядом с именем)
5. Если все оплатили → статус event = `paid`

**Логика «Отменить оплату»:**
1. Определить пользователя
2. Найти его запись в Payments
3. Установить is_paid = false, paid_at = null
4. Обновить сообщение (убрать ✓)
5. Если статус event был `paid` → вернуть `finalized`

---

## Сценарий 9: Напоминание должникам

**Триггер:** n8n вызывает `POST /check-payments` раз в день (в 12:00)

**Логика:**
1. Найти все events в статусе `finalized` с неоплаченными участниками
2. Для каждого участника проверить: сколько напоминаний уже отправлено по этому event
3. Если < 3 и прошёл минимум 1 день после finalize → отправить напоминание
4. По четвергам дополнительно отправить еженедельную сводку долга

**Подробная логика нотификаций — см. раздел «Система нотификаций»**

---

## Сценарий 10: Изменение настроек

**Актор:** Админ

**Способ:** Напрямую в Notion (без команд бота)

**Таблица Settings в Notion:**

| key | value |
|-----|-------|
| court_price | 2000 |
| timezone | Europe/Belgrade |
| reminder_hour | 12 |

Бот читает настройки из Notion при каждом действии, где они нужны.

---

## Сценарий 11: Просмотр истории и долгов

### Команды для всех участников

```
/my history <filter>
/my debt
```

**Форматы фильтра для history:**
- `/my history 10` — последние 10 занятий
- `/my history 12.24` или `12.2024` или `12-2024` или `2024-12` — за декабрь 2024
- `/my history 10.24-12.24` — за период октябрь–декабрь 2024

**Пример вывода `/my history 5`:**
```
📋 Твоя история (последние 5):

21.01 Вт — 1000 ₽ ✓
18.01 Сб — 1500 ₽ ✓
14.01 Вт — 1000 ₽ (не оплачено)
11.01 Сб — 2000 ₽ ✓
07.01 Вт — 1000 ₽ ✓
```

**Пример вывода `/my debt`:**
```
💰 Твой долг: 1000 ₽

14.01 Вт — 1000 ₽
```

### Команды для админа

```
/admin debts
/admin history @username <filter>
```

**Пример вывода `/admin debts`:**
```
💰 Должники:

@vasya — 2500 ₽
@petya — 1000 ₽
Иван Иванов — 1500 ₽

Всего: 5000 ₽
```

**`/admin history`** — тот же формат фильтра, что и `/my history`

### Погашение долга (админ)

```
/admin repay @username <amount>
```

**Пример:**
```
/admin repay @vasya 1000
→ Долг @vasya уменьшен на 1000 ₽. Остаток: 1500 ₽
```

Погашение уменьшает общую сумму долга без привязки к конкретным событиям.

---

## Система нотификаций

### 1. Event не рассчитан

**Триггер:** Прошло 1.5 часа после начала занятия, но никто не нажал Finalize

**Куда:** Общий чат

**Частота:** Каждые 2 часа

**Прекращение:** После нажатия Finalize

**Формат:**
```
⚠️ Сквош 21 января завершён, но не рассчитан. Нажмите ✅ Finalize.
```

---

### 2. Напоминание об оплате за event

**Триггер:** Event в статусе `finalized`, участник не оплатил

**Начало:** На следующий день после finalize

**Частота:** Каждый день

**Максимум:** 3 напоминания на event

**Куда:** 
1. Сначала в личку
2. Если не получилось — в общий чат с тегом

**Формат (личка):**
```
⏰ Напоминание об оплате за сквош (21 января)

Сумма: 1000 ₽

После перевода отметь оплату в чате: [ссылка на сообщение]
```

**Формат (общий чат, fallback):**
```
⏰ @vasya, напоминание об оплате за сквош (21 января) — 1000 ₽
```

---

### 3. Еженедельная сводка долга

**Триггер:** Четверг, 12:00

**Условие:** У участника есть непогашенный долг (любой)

**Куда:** 
1. Сначала в личку
2. Если не получилось — в общий чат с тегом

**Формат:**
```
📊 Еженедельная сводка

Твой общий долг: 3500 ₽

• 21.01 — 1000 ₽
• 18.01 — 1500 ₽
• 14.01 — 1000 ₽

Отметить оплату можно в соответствующих сообщениях в чате.
```

---

### Сводная таблица нотификаций

| Нотификация | Триггер | Куда | Частота | Лимит |
|-------------|---------|------|---------|-------|
| Event не рассчитан | 1.5ч после начала | Общий чат | Каждые 2 часа | До finalize |
| Оплата за event | День после finalize | Личка → Общий | Каждый день | 3 раза |
| Сводка долга | Четверг 12:00 | Личка → Общий | Раз в неделю | — |

---

## Сценарий 12: Тестовые команды

**Актор:** Только админ

**Где работают:** Только в тестовом чате

**Тестовая среда:**
- Отдельные таблицы в Notion (Scaffolds_Test, Events_Test, Participants_Test, EventParticipants_Test, Payments_Test, Settings_Test)
- Бот определяет среду по chat_id (тестовый чат → тестовые таблицы)
- Чёткое разделение: prod и test никогда не пересекаются

**Команды:**

```
/test scaffold_create — создать тестовый scaffold
/test scaffold_list — показать scaffolds в тестовой среде
/test event_create — создать event из scaffold или ad-hoc
/test event_announce — анонсировать event
/test event_finish — перевести event в статус finished
/test event_finalize — симулировать finalize
/test payment_remind — отправить напоминание об оплате
/test weekly_remind — отправить еженедельную сводку
/test cleanup — удалить все тестовые данные
```

**Пример тестового сценария:**
```
/test scaffold_create
→ Создан тестовый scaffold sc_test_1: Tue 21:00, 2 корта

/test event_create sc_test_1
→ Создан event ev_test_1 из scaffold sc_test_1

/test event_announce ev_test_1
→ Анонс отправлен в тестовый чат

(нажать кнопки «Я иду» в чате)

/test event_finalize ev_test_1
→ Event финализирован, сообщение об оплате отправлено

/test payment_remind ev_test_1
→ Напоминание отправлено

/test cleanup
→ Все тестовые данные удалены
```

---

## Формула расчёта

```
сумма_к_оплате = стоимость_корта × количество_кортов × твои_участия / сумма_всех_участий
```

**Пример:**
- Стоимость корта: 2000
- Кортов: 2
- Участники: Паша (1), Вася (2), Петя (1) → сумма участий = 4
- Паша платит: 2000 × 2 × 1 / 4 = 1000
- Вася платит: 2000 × 2 × 2 / 4 = 2000
- Петя платит: 2000 × 2 × 1 / 4 = 1000

---

## Настройки

| Параметр | Значение по умолчанию | Описание |
|----------|----------------------|----------|
| court_price | 2000 | Стоимость одного корта |
| timezone | Europe/Belgrade | Часовой пояс |
| default_announce_time | 12:00 previous day | Когда создавать event из scaffold |
| min_announce_hours | 24 | Минимум часов до занятия для анонса |

---

## Нефункциональные требования

### Тестирование

**Ручное тестирование:**
- Отдельный тестовый чат в Telegram
- Тестовые команды `/test *` (только админ)
- Отдельные таблицы в Notion (*_Test)

**E2E тестирование:**
- Playwright + Telegram Web (web.telegram.org)
- Тестовый Telegram-аккаунт
- Тесты пишут команды в тестовый чат и проверяют результат

**Авторизация тестового аккаунта:**
- Сохранение сессии Playwright между запусками
- Или ручная авторизация перед первым запуском тестов

### Логирование
- Технический чат для логов всех действий
- Логируется: постинг сообщений, клики по кнопкам, напоминания, ошибки

### Тестовые команды

Полный список — см. Сценарий 12.

```
/test scaffold_create
/test scaffold_list
/test event_create
/test event_announce
/test event_finish
/test event_finalize
/test payment_remind
/test weekly_remind
/test cleanup
```

---

## Структура Notion

### Production-таблицы

### Таблица: Scaffolds
| Поле | Тип |
|------|-----|
| id | Title |
| day_of_week | Select |
| time | Text |
| default_courts | Number |
| is_active | Checkbox |
| announce_hours_before | Number |

### Таблица: Events
| Поле | Тип |
|------|-----|
| id | Title |
| scaffold_id | Relation → Scaffolds |
| datetime | Date |
| courts | Number |
| status | Select (announced, finished, paid) |
| telegram_message_id | Text |
| payment_message_id | Text |

### Таблица: Participants
| Поле | Тип |
|------|-----|
| id | Title |
| telegram_username | Text |
| telegram_id | Text |
| display_name | Text |

### Таблица: EventParticipants
| Поле | Тип |
|------|-----|
| event_id | Relation → Events |
| participant_id | Relation → Participants |
| participations | Number (по умолчанию 1) |

### Таблица: Payments
| Поле | Тип |
|------|-----|
| event_id | Relation → Events |
| participant_id | Relation → Participants |
| amount | Number |
| is_paid | Checkbox |
| paid_at | Date |
| reminder_count | Number (по умолчанию 0, максимум 3) |

### Таблица: Settings
| Поле | Тип |
|------|-----|
| key | Title |
| value | Text |

### Test-таблицы

Идентичная структура с суффиксом `_Test`:
- Scaffolds_Test
- Events_Test
- Participants_Test
- EventParticipants_Test
- Payments_Test
- Settings_Test

Бот выбирает набор таблиц по chat_id:
- Тестовый чат → *_Test таблицы
- Основной чат → Production таблицы

---

## API Endpoints бота

| Endpoint | Метод | Описание | Вызывается из |
|----------|-------|----------|---------------|
| /health | GET | Healthcheck | n8n (каждые 5 мин) |
| /check-events | POST | Проверить, нужно ли создать/анонсировать events | n8n (каждые 15 мин) |
| /check-payments | POST | Проверить должников, отправить напоминания | n8n (раз в день) |

---

## E2E тестирование

**Стек:**
- Playwright
- Telegram Web (web.telegram.org)
- Тестовый Telegram-аккаунт
- Тестовый чат с ботом

**Авторизация тестового аккаунта:**
*TODO: продумать подход — сохранение сессии, environment variables для кода авторизации*

---

## Open Questions — RESOLVED

1. **Нужна ли возможность редактировать scaffold (не только toggle/remove)?**
   → Нет, админ поправит руками в Notion если нужно.

2. **Что делать с историческими events при удалении scaffold?**
   → Ничего, events остаются.

3. **Нужен ли откат отметки «Оплатил»?**
   → Да, нужен. См. сценарий 8.