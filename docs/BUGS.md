* Active wizard is not discarded if a new command is posted.
* /start at the bot's does not register a user in the database. Recheck `+participant` command while creating/editing a private event/scaffold.
* Service logs should contain logs about ALL the business events.